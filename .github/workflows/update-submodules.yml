name: Update Submodule

on:
  workflow_call:
    secrets:
      GH_PAT:
        required: true

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository with submodules
        uses: actions/checkout@v5
        with:
          submodules: recursive
          token: ${{ secrets.GH_PAT }} # PATを指定

      - name: Update submodules to latest
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          git submodule foreach --recursive '
            echo "=== Processing: $name ==="

            branch=$(git config -f "$toplevel/.gitmodules" "submodule.$name.branch")

            if [ -z "$branch" ]; then
              echo "ERROR: branch is not configured for $name"
              exit 1
            fi

            echo "Target branch: $branch"

            git fetch origin "$branch"
            git checkout -B "$branch" "origin/$branch"
            git reset --hard "origin/$branch"
            git clean -fd

            echo "Updated to:"
            git log -1 --oneline
          '

      - name: Check for changes, commit and push
        run: |
          # 変更（差分）があるかどうかをチェック
          git diff --quiet && git diff --staged --quiet
          if [ $? -eq 0 ]; then
            echo "変更はないから、コミット＆プッシュはスキップするで。"
          else
            echo "変更を検知したから、コミット＆プッシュを実行するで。"
            git add .
            git commit -m "chore: auto-update submodules to latest [skip ci]"
            git push
          fi
